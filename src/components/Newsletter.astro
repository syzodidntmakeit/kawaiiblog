---

---

<div class="newsletter-card">
  <h3>💌 Join the Kawaii Club</h3>
  <p>
    Get the latest rants, tech tips, and music recs directly in your inbox. No
    spam, just vibes.
  </p>
  <form
    action="https://buttondown.email/api/emails/embed-subscribe/PLACEHOLDER_USER"
    method="post"
    target="popupwindow"
    onsubmit="window.open('https://buttondown.email/PLACEHOLDER_USER', 'popupwindow')"
    class="embeddable-buttondown-form"
  >
    <input type="email" name="email" placeholder="your@email.com" required />
    <button type="submit">Subscribe</button>
  </form>
</div>

<style>
  .newsletter-card {
    background: linear-gradient(
      135deg,
      rgba(255, 192, 203, 0.1),
      rgba(167, 199, 231, 0.1)
    );
    border: 1px solid var(--pink);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin-top: var(--spacing-xl);
    text-align: center;
  }

  .newsletter-card h3 {
    color: var(--pink);
    margin-top: 0;
  }

  form {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
    margin-top: 1rem;
    flex-wrap: wrap;
  }

  input {
    padding: 0.5rem 1rem;
    border-radius: var(--radius-sm);
    border: 1px solid var(--muted);
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-primary);
  }

  button {
    padding: 0.5rem 1.5rem;
    background: var(--pink);
    color: var(--bg);
    border: none;
    border-radius: var(--radius-sm);
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.1s;
  }

  button:hover {
    transform: scale(1.05);
  }
</style>
